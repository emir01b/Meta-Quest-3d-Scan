<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.meta3d.scanner">

    <!-- Camera permission for passthrough camera access -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Quest-specific headset camera permission -->
    <uses-permission android:name="horizonos.permission.HEADSET_CAMERA" />

    <!-- Network permissions for WebSocket communication -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- VR headtracking feature -->
    <uses-feature
        android:name="android.hardware.vr.headtracking"
        android:required="true"
        android:version="1" />

    <!-- Quest hardware feature -->
    <uses-feature
        android:name="oculus.software.handtracking"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:label="Meta3D Scanner"
        android:icon="@mipmap/app_icon">

        <!-- Mark as Quest app -->
        <meta-data
            android:name="com.oculus.supportedDevices"
            android:value="quest3|quest3s" />

        <!-- Enable passthrough -->
        <meta-data
            android:name="com.oculus.MR_ENABLED"
            android:value="true" />

        <!-- Require passthrough support -->
        <meta-data
            android:name="com.oculus.passthrough_support"
            android:value="required" />

        <activity
            android:name="com.unity3d.player.UnityPlayerActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:launchMode="singleTask"
            android:configChanges="density|fontScale|keyboard|keyboardHidden|layoutDirection|locale|mnc|mcc|navigation|orientation|screenLayout|screenSize|smallestScreenSize|uiMode|touchscreen"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />

                <!-- VR intent category -->
                <category android:name="com.oculus.intent.category.VR" />
            </intent-filter>
        </activity>
    </application>
</manifest>
